include:
  - board: nice_nano_v2
    shield: tester_pro_micro
    extra_cmake_args:
      -DCONFIG_CUSTOM_LINKER_SCRIPT=y
      -DCONFIG_FLASH_BASE_ADDRESS=0x26000
      -DCONFIG_NORDIC_QSPI_NOR=n
      -DCONFIG_BOOTLOADER_MCUBOOT=n
